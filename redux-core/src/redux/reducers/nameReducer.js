const initialState = {
    fullName: "Tran Van Dung"
}

const nameReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default nameReducer;